version: '3.8'

services:
  dashboard-api:
    build: .
    container_name: iot-backend
    ports:
      - "80:8000" # Acessível em http://localhost
    volumes:
      - ./data:/app/data # Pasta local onde o SQLite será salvo permanentemente
    restart: always